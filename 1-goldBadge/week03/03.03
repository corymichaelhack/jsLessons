Today's Notes:

Firebase Deploy for Assignment 4

firebase.google.com
  GO TO CONSOLE 
  Add project
  Add project name => explain how unique name gets no characters at end
    Allow Google Analytics
    Accept the terms
    Create project
    Click continue
    Go to Hosting on SideBar (globe)
    Click 'Get Started' 
  Explain what you can do with Firebase => better than GitHub Pages

npm install -g firebase-tools (sudo npm install -g firebase-tools)

navigate to project folder in terminal 
firebase login => google creds

firebase init
  using arrow keys, 
    - select (space) Hosting: Configure and deploy Firebase Hosting sites, Enter to confirm
    - select Use an existing project
    - select the project you want to deploy
  public directory = ./
  rewrite = N (for NO)
  override = N (for NO) 

Show new files added in app
  .firebaserc
  404.html
  firebase.json:
    {
      "hosting": {
        "public": "./",
        "ignore": [
          "firebase.json",
          "**/.*",
          "**/node_modules/**"
        ]
      }
    }

git add .
git commit -m "message"
git push
firebase deploy 

Repl.it Assessment 5 - DUE BY TOMORROW MORNING

